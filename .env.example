# Environment Configuration
ENVIRONMENT=development

# Database
DATABASE_URL=sqlite:///./deadman_switch.db
# For production with PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost/deadman_switch

# Security
SECRET_KEY=your-secret-key-change-in-production-make-it-long-and-random
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server
HOST=0.0.0.0
PORT=8000

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Deadman Switch

# Redis (for background tasks)
REDIS_URL=redis://localhost:6379/0

# Render.com specific
# These will be set automatically by Render
# DATABASE_URL will be provided by Render for PostgreSQL
# PORT will be set by Render
